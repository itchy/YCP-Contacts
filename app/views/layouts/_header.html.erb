<div id="header">
	HEADER <br />
	<% if current_user %>
		<%= link_to("Logout", session_logout_path )%> <br />
		<%= link_to("My Profile", profile_path(current_user.profile))%><br />
		<% if current_user.admin? %>
			<%= link_to("Manage Users", "/" )%>
		<% end %>	
	<% end %>
	
</div>